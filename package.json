{
  "name": "white-room-button-game",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny mobile 3D room game packaged as an Android app with Capacitor.",
  "scripts": {
    "sync:android": "npx cap sync android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
